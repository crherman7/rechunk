import {ChunkConfig} from './types';

/**
 * Defines a configuration for rechunk.
 * Type-guards the configuration for Re.Chunk.
 *
 * @param config - The configuration object of type ChunkConfig.
 * @returns The provided configuration.
 */
export function defineConfig(config: ChunkConfig) {
  return config;
}
